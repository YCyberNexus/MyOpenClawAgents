# Superseded Plan

This historical state-machine plan has been superseded by the current `req_executor` contract.

Current contract summary:

- Issue content is the Claude Code prompt.
- Runtime state lives under `.req_executor/`.
- There are no runtime basename, project data directory, or UI account-pool inputs.
- Current behavior is documented in `skills/gitlab_issue_campaign_dispatcher/references/`.
