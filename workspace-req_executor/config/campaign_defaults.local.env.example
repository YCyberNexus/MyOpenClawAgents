# 本机/单 runner 覆盖示例。复制为 campaign_defaults.local.env 后按需填写；
# campaign_defaults.local.env 已被仓库忽略，不要提交真实 token 或本机路径。

# GITLAB_TOKEN=
# REPO_PARENT_PATH=/Users/<user>/openclaw-local-data
# GROUP=claw_gitlab
