# Legacy placeholder.
#
# The dispatcher no longer reads this file. UI test accounts are maintained by
# the test team in the cloned project repo at:
#
#   ${REPO_PATH}/${UI_ACCOUNTS_RELPATH}   (default: ifp-data/ifp-common/ifp_users.json)
#
# The relpath is resolved under the project checkout root, NOT under
# ${REPO_PATH}/${DATA_BASENAME}/. See config/README.md and
# scripts/load_ui_accounts.sh for the active contract.
