# req_dispatcher 多 project 路由表（deployment-time pin）。
# 在每台部署本 agent 的 runner 上编辑一次；agent 运行时不改写本文件。
#
# 用途：少数 project 需要专属 req_executor 部署时，在本表里写覆盖项。
# 默认行为由 config/dispatcher.env 的 DEFAULT_EXECUTOR_AGENT 控制：所有形态合法的
# GitLab project（group/project）都默认路由到该 executor。消费方：scripts/route_project.sh。
#
# 行格式：每行一条 `PROJECT=AGENT`
#   - PROJECT  git_issuer 返回的 group/project（含 `/`，故本文件不能被 shell `source`，由
#              route_project.sh 逐行手解；匹配是「对 PROJECT 整体精确相等」，无前缀/正则/大小写折叠）。
#   - AGENT    该 project 对应的 req_executor 部署 agent 名。
#   - `#` 起头行与空行忽略；`PROJECT = AGENT`（等号两侧带空格）也容忍。
#
# 未命中语义：先回落到 DEFAULT_EXECUTOR_AGENT；只有默认执行器未配置时才输出 `__NO_ROUTE__`。
# PROJECT 必须是完整 `group/project`；缺 group 的裸仓库名属于 git_issuer 返回形态错误。
#
# 文件格式错（某行无 `=`、PROJECT/AGENT 为空）或本文件缺失 = 部署期配置写错：route_project.sh
# exit 2，orchestrator 走 No-Fallback（分类/记录/停），不当成 no-route。

# 示例（按实际部署改；默认执行器已覆盖所有合法 project，通常不需要写项目行）：
# ai-infra/special_project=req_executor_special
