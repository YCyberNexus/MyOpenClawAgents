# git_issuer deployment pin template.
# Copy to gitlab.env on each runner and fill GITLAB_TOKEN locally.

GITLAB_HOST=localhost:8081
GITLAB_API_PROTOCOL=http
GITLAB_TOKEN=
DEFAULT_ENTRY_LABEL=todo
STATE_ROOT=/Users/yuanchenxiang/openclaw-local-data/git_issuer
