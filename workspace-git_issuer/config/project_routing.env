# Format:
#   <group>/<project>|alias1,alias2,alias3
#
# Matching is fixed-string and case-sensitive. The parser also matches the
# project slug after the final slash, even if the slug is not listed as an
# alias. Unknown project text must fail instead of being guessed.

claw_gitlab/px_ifp_hulat_test|px_ifp_hulat_test,ifp,hulat
